package com.example.cookingapp.data.entity

enum class SourceType { USER, URL, API }
enum class UsageStatus { USED, MISSING, SUBSTITUTED }
enum class AdjustmentReason { COOK_DEDUCT, MANUAL_PANTRY_EDIT }